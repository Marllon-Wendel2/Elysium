import { Room } from 'src/rooms/room.entity';

// eslint-disable-next-line @typescript-eslint/require-await
export async function arthurEscolhido(
  room: Room,
  context: EffectContext,
): Promise<boolean> {
  return true;
}
